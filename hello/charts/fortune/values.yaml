# fortune/values.yaml 파일

replicaCount: 1

image:
  # image 저장소의 위치는 harbor 를 바라 보도록 한다 
  repository: 172.16.8.40/library/fortune
  tag: "latest"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8000 # FastAPI 포트